<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Music Play Pause Button</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0.1/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <script src="/static/js/audio.js"></script>
    <script src="/static/js/script.js"></script>
</head>
<body class="flex flex-col items-center justify-center h-screen bg-gray-100 m-0">
    <!-- Logo -->
    <img src="static/images/logo.png" alt="Logo" class="absolute top-5 left-5 z-10 w-56 h-auto">
    
    <!-- Login/Signup button -->
    <div class="absolute top-5 right-5 z-20">
        <a href="{{ url_for('login') }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">Login/Signup</a>
    </div>

    <!-- Play/Pause button -->
    <button id="playPauseBtn" onclick="togglePlayPause()" class="play-pause-button bg-red-500 hover:bg-red-700 text-white font-bold py-4 px-4 rounded-full w-16 h-16 flex items-center justify-center focus:outline-none" style="position: absolute; left: calc(5px + 225px + 90px); top: 5px; z-index: 10;">
        <i class="fas fa-play"></i>
    </button>

    <!-- Long sheet of paper for plain text -->
    <div class="absolute top-1/2 left-1/2 w-2/3 h-2/3 bg-white shadow-xl transform -translate-x-1/2 -translate-y-1/2 p-8 overflow-auto z-5">
        <!-- Plain text can go here -->
    </div>

    <!-- Canvas for Particle Effect -->
    <canvas id="particleCanvas" class="absolute top-0 left-0 pointer-events-none z-0 w-full h-full"></canvas>
</body>
</html>