<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Music Play Pause Button</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0.1/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <script src="/static/js/audio.js"></script>
    <script src="/static/js/script.js"></script>
    <style>
        /* Play/pause button positioning relative to the logo */
        .play-pause-button {
            position: absolute;
            left: calc(5px + 225px + 90px); /* Increased additional space from 40px to 90px */
            top: 5px; /* Same vertical level as logo */
            z-index: 10;
        }
    </style>    
</head>
<body class="flex flex-col items-center justify-center h-screen bg-gray-100 m-0">
    <!-- Logo -->
    <img src="static/images/logo.png" alt="Logo" class="absolute top-5 left-5 z-10 w-56 h-auto">
    
    <!-- Login/Signup button -->
    <div class="absolute top-5 right-5 z-20">
        <a href="{{ url_for('login') }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">Login/Signup</a>
    </div>

    <!-- Play/Pause button -->
    <button id="playPauseBtn" onclick="togglePlayPause()" class="play-pause-button bg-red-500 hover:bg-red-700 text-white font-bold py-4 px-4 rounded-full w-16 h-16 flex items-center justify-center focus:outline-none">
        <i class="fas fa-play"></i>
    </button>

    <!-- Canvas for Particle Effect -->
    <canvas id="particleCanvas" class="absolute top-0 left-0 pointer-events-none z-0 w-full h-full"></canvas>
</body>
</html>
