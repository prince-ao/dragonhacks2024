<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0.1/dist/tailwind.min.css" rel="stylesheet">
    <style>
        /* Adding a subtle background gradient and playful accents */
        body {
            background: linear-gradient(to right, #e3f2fd, #bbdefb);
            font-family: 'Comic Sans MS', 'Comic Neue', cursive; /* playful font */
        }
        /* Enhancing input focus effect */
        input:focus {
            box-shadow: 0 0 0 3px rgba(29,78,216,0.3);
            transition: box-shadow 0.3s ease-in-out;
        }
        /* Button interaction */
        button:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(29,78,216,0.3);
            transition: all 0.2s ease-out;
        }
        /* Style for the pencil sticker */
        .pencil-sticker {
            width: 60px;
            height: 60px; /* adjust as needed */
            background: url('/static/images/pencil.png') no-repeat center center;
            background-size: contain;
            margin-top: -30px; /* half of height for negative offset */
            margin-right: -30px; /* half of width for negative offset */
            float: right;
        }
        /* Login container specific styling */
        .login-container {
            position: relative;
            padding-top: 40px; /* Added space for the pencil sticker */
        }
    </style>
</head>
<body class="flex items-center justify-center h-screen">
    <!-- Login container with adjusted padding -->
    <div class="bg-white p-8 rounded-lg shadow-lg max-w-md w-full space-y-6 login-container">
        <!-- Pencil sticker inside the container -->
        <div class="pencil-sticker"></div>
        <h1 class="text-3xl font-bold text-center text-gray-800 mb-10">Login to Your Account</h1>
        <form action="/login" method="POST">
            <!-- Email Input -->
            <div class="mb-5">
                <label for="email" class="block text-sm font-medium text-gray-700">Email</label>
                <input type="email" id="email" name="email" class="mt-1 block w-full px-4 py-3 border border-gray-300 rounded-md shadow-sm focus:outline-none" required>
            </div>
            <!-- Password Input -->
            <div class="mb-5">
                <label for="password" class="block text-sm font-medium text-gray-700">Password</label>
                <input type="password" id="password" name="password" class="mt-1 block w-full px-4 py-3 border border-gray-300 rounded-md shadow-sm focus:outline-none" required>
            </div>
            <!-- Remember Me and Forgot Password -->
            <div class="flex items-center justify-between mb-5">
                <div class="flex items-center">
                    <input id="remember_me" type="checkbox" class="h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded">
                    <label for="remember_me" class="ml-2 block text-sm text-gray-900"> Remember me</label>
                </div>
                <a href="#" class="text-sm text-blue-600 hover:text-blue-800">Forgot your password?</a>
            </div>
            <!-- Sign In Button -->
            <button type="submit" class="w-full bg-blue-600 hover:bg-blue-800 text-white font-bold py-3 px-4 rounded-lg focus:outline-none">Sign In</button>
            <!-- Link to Signup Page -->
            <p class="mt-8 text-center text-sm">
                Don't have an account?
                <a href="/signup" class="text-blue-600 hover:text-blue-800 font-semibold">Sign up</a>
            </p>
        </form>
    </div>
</body>
</html>
